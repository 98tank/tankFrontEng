import { NestedOrderPipe } from './nested-order.pipe';

describe('NestedOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
